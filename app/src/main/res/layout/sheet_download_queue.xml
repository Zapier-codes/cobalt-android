<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/cobalt_surface_sidebar"
    android:minHeight="200dp">

    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="8dp"
        android:background="@color/cobalt_input_border" />

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        app:tabGravity="fill"
        app:tabMode="fixed"
        app:tabTextColor="@color/cobalt_text_secondary"
        app:tabSelectedTextColor="@color/cobalt_text_primary"
        app:tabIndicatorColor="@color/cobalt_accent_blue"
        app:tabBackground="@android:color/transparent"
        android:background="@color/cobalt_surface_sidebar"
        app:tabTextAppearance="@style/TextAppearance.Cobalt.Mono" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:paddingBottom="16dp" />

    <TextView
        android:id="@+id/tvEmpty"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:gravity="center"
        android:text="no downloads yet"
        android:textColor="@color/cobalt_text_secondary"
        android:fontFamily="@font/ibm_plex_mono_regular"
        android:textSize="13sp"
        android:visibility="gone" />

    <!-- Phase 17: skeleton placeholder for the brief window before
         DownloadQueueViewModel's Room-backed LiveData delivers its first
         emission (Room queries run off the main thread, so there's a real
         if small gap here, not a hypothetical one). Visible on sheet open;
         hidden for good once DownloadQueueSheet observes a first value on
         either allDownloads or activeDownloads — see hasLoadedOnce there. -->
    <LinearLayout
        android:id="@+id/skeletonDownloads"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:visibility="visible">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingVertical="8dp">

            <View
                android:layout_width="220dp"
                android:layout_height="14dp"
                android:layout_marginBottom="6dp"
                android:background="@drawable/bg_skeleton_block" />

            <View
                android:layout_width="120dp"
                android:layout_height="11dp"
                android:background="@drawable/bg_skeleton_block" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingVertical="8dp">

            <View
                android:layout_width="180dp"
                android:layout_height="14dp"
                android:layout_marginBottom="6dp"
                android:background="@drawable/bg_skeleton_block" />

            <View
                android:layout_width="120dp"
                android:layout_height="11dp"
                android:background="@drawable/bg_skeleton_block" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
